import { AuditViewerView } from "../../components/audit-viewer-view";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function AuditViewerPage() {
  return <AuditViewerView />;
}
