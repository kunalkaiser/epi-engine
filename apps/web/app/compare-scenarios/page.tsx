import { CompareScenariosView } from "../../components/compare-scenarios-view";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function CompareScenariosPage() {
  return <CompareScenariosView />;
}
